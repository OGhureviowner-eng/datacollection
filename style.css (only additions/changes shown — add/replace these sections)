/* Add these new styles (or append to your existing style.css) */

/* Search and filter controls */
.controls {
  text-align: center;
  margin: 30px 0;
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  gap: 15px;
}

.controls input, .controls select {
  padding: 10px;
  background: #222;
  border: 1px solid #333;
  color: var(--highlight);
  border-radius: 8px;
  font-size: 1rem;
  min-width: 250px;
}

/* Zone header clickable and collapsible */
.zone-header {
  cursor: pointer;
  user-select: none;
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 15px;
  background: var(--secondary);
  border-radius: 16px;
  margin-bottom: 10px;
}

.zone-header:hover {
  background: #222;
}

.zone-count {
  background: var(--accent);
  color: var(--primary);
  padding: 5px 12px;
  border-radius: 20px;
  font-size: 0.9rem;
}

.grid {
  transition: all 0.3s ease;
}

.card {
  opacity: 1;
  transition: opacity 0.3s ease, transform 0.3s ease;
}

.card.hidden {
  opacity: 0;
  transform: translateY(20px);
  height: 0;
  padding: 0;
  margin: 0;
  overflow: hidden;
}

/* Arrow indicator for collapsible */
.arrow {
  font-size: 1.5rem;
  transition: transform 0.3s ease;
}

.arrow.collapsed {
  transform: rotate(-90deg);
}
